#pragma once
#ifndef FCT_FRAC_H
#define FCT_FRAC_H

typedef unsigned char uint8;	// 8 bits non signé
typedef signed char int8;		// 8 bits signé
typedef unsigned short uint16;	// 16 bits non signé
typedef signed short int16;		// 16 bits signé
typedef unsigned long uint32;	// 32 bits non signé
typedef signed long int32;		// 32 bits signé

double TestFonction();
double FctFactorielle(uint8 *byV_ValFrac);
int fib(int n);

#endif